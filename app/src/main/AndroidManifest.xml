<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Needed so the per-app capture picker can enumerate all installed apps.
         Since Android 11 (API 30) package visibility is filtered by default. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pivot">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Egress SOCKS5 listener (from microsocks). Resolves DNS on-device and
             connects out through the phone's own interface. -->
        <service
            android:name=".egress.EgressService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-controlled local SOCKS5 proxy listener" />
        </service>

        <!-- Capturing VPN (from socksdroid). Single process; no JNI fd handoff. -->
        <service
            android:name=".vpn.PivotVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-controlled traffic-capturing SOCKS5 VPN" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- adb/automation control surface. Exported but gated by DUMP, which only the
             shell user (adb) and the system hold — ordinary apps can't obtain it — so in
             practice only `adb shell am broadcast …` can drive it. -->
        <receiver
            android:name=".control.ControlReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="eu.matscheko.pivot.action.CONFIGURE" />
                <action android:name="eu.matscheko.pivot.action.EGRESS_START" />
                <action android:name="eu.matscheko.pivot.action.EGRESS_STOP" />
                <action android:name="eu.matscheko.pivot.action.VPN_START" />
                <action android:name="eu.matscheko.pivot.action.VPN_STOP" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
